POSTGRES_USER=pharma_admin
POSTGRES_PASSWORD=ЗАМЕНИТЕ_НА_НАДЁЖНЫЙ_ПАРОЛЬ
POSTGRES_DB=pharma_wms
DATABASE_URL=postgresql://pharma_admin:ЗАМЕНИТЕ_НА_НАДЁЖНЫЙ_ПАРОЛЬ@db:5432/pharma_wms
SECRET_KEY=                          # минимум 32 символа: python3 -c "import secrets; print(secrets.token_hex(32))"
ACCESS_TOKEN_EXPIRE_MINUTES=480
ENVIRONMENT=production               # production | development
ALLOWED_ORIGINS=http://localhost     # через запятую если несколько
REGISTER_ENABLED=false               # true только в dev-режиме
